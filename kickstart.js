/*jslint node: true, indent: 2*/

/*
  This file starts the server as defined in `server/server.coffee`

  It basically just exists so that everything 
  else can be written in CoffeeScript.
  
  Poor file.
*/

require("coffee-script");
require("./server/server");